"""
The `app.routers` package.

Groups all our FastAPI "routers" (collections of related endpoints)
together. Right now we only have `tasks.py`, but in a bigger app you
might add `users.py`, `auth.py`, etc. and each one would live here.
"""
