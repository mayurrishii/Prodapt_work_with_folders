"""
The `app` package.

This file marks the `app` folder as a Python package so we can do
imports like `from app.database import get_db` or run the server with
`uvicorn app.main:app`. It intentionally stays empty - it has no
responsibility of its own, it just makes the package importable.
"""
